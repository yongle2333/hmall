spring:
  application:
    name: gateway #微服务名字
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.221.142:8848   #nacos地址
      config:
        file-extension: yaml    #文件后缀名
        shared-configs:
          - data-id: shared-log.yaml

