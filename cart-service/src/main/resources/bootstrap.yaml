spring:
  application:
    name: cart-service #微服务名字
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr:    #nacos地址
      config:
        file-extension: yaml    #文件后缀名
        shared-configs:
          - data-id: shared-jdbc.yaml
          - data-id: shared-log.yaml
          - data-id: shared-swagger.yaml

